apiVersion: v1
kind: Service
metadata:
  name: bitwave-svc
  labels:
    app: bitwave
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: bitwave
